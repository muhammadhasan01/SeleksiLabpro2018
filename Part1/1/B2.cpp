#include <bits/stdc++.h>

using namespace std;

int main() { 
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int x = 0, y = 1, z = 0;
    long long a, b, c;
    cout << x << " " << y << " " << z << '\n';
    a = x, b = y, c = z;
    // long long maks = max(a, max(b, c));
    // if ((a + b) > (b + c)) {
    //     if ((a + c) > (b + c)) {
    //         assert(a == maks);
    //     } else {
    //         assert(b == maks);
    //     }
    // } else if ((a + b) < (b + c)) {
    //     if ((a + c) > (a + b)) {
    //         assert(c == maks);
    //     } else {
    //         assert(b == maks);
    //     }
    // } else {
    //     if ((a + b) > (b + c)) {
    //         assert(a == maks);
    //     } else {
    //         assert(c == maks);
    //     }
    // }

    return 0;
}